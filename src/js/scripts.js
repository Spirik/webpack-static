// jQuery is loaded via ProvidePlugin so no need for the following inclusions
// import jQuery from 'jquery';
// or
// window.jQuery = window.$ = require('jquery');

$(function() {
  console.log("My <body> is ready!");
});
